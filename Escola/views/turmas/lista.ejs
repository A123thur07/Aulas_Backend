<%- include('../includes/header.ejs', {titulo: 'Lista de turmas' }) %>

    <main>
        <h2>Lista de turmas</h2>
        <a href="/turmas/novo" class="btn btn-principal">Adicionar Turmas</a>
        <form action="/turmas/listar" method="get" class="form form-consulta">
            <input type="text" name="busca" placeholder="Buscar por turma">
            <select name="ordem">
                <option value="nome_turma">Ordenar por nome de turma</option>
            </select>
            <button type="submit" class="btn btn-principal">Buscar</button>
        </form>
        <table class="dados-table">
            <thead>
                <tr>
                    <th>Nome </th>
                    <th>Ações </th>
                </tr>
            </thead>
            <tbody>
                <% for( let turmas of dadosTurmas) { %>      
                    <% if(turmas.ativo == true)  { %>            
                <tr>
                    <td><%= turmas.nome_turma %></td>
                    <td>
                        <a href="/turmas/editar/<%= turmas.id_turma %>" class="btn btn-editar">Editar</a>
                        <form action="/turmas/excluir/<%= turmas.id_turma %>" method="POST" class="acoes">
                            <button type="submit" class="btn btn-delete">Excluir</button>
                        </form>
                    </td>
                </tr>
                    <% } %>
                <% } %>
            </tbody>
        </table>
    </main>

<%- include('../includes/footer.ejs') %>